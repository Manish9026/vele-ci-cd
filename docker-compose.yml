version: '3.8'

services:
  backend:
    image: your-dockerhub-username/mern-backend:${TAG}
    env_file:
      - ./server/.env
    ports:
      - "5000:5000"
    depends_on:
      - mongodb

  frontend:
    image: your-dockerhub-username/mern-frontend:${TAG}
    env_file:
      - ./client/.env
    ports:
      - "3000:3000"
    depends_on:
      - backend

  mongodb:
    image: mongo:6
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
